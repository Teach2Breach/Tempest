# `conduit` (deprecated)

This crate is the **legacy** operator **TUI** (ratatui). **Use [`conduit_gui`](../conduit_gui/)** (Dioxus) for new work. This directory stays in the tree for compatibility and may receive minimal server-protocol fixes, but the **primary** operator experience is the GUI; see the root `README.md` and `gui_plan.md`.

**Known issues** (unchanged from historical notes): output display can glitch; the GUI is the supported path for ongoing output/polling work.
